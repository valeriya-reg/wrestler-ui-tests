# Requirements
- NodeJS: v8.x.x
- npm: 5.8.x
- Chrome browser

# Preparation
Install dependencies:
- `npm install -g protractor`
- `webdriver-manager update`

# Test
Start up a server:
- `webdriver-manager start`

Run tests: 
- `protractor conf.js`